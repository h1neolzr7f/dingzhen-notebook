# 粉笔学习数据处理系统 · 可使用成品交付

版本：1.3.4（丁真笔记本 · 一键包 + 开源）  
交付目录：本文件夹（`fenbi-study-pipeline-usable`）

## 1. 你拿到了什么

| 组件 | 路径 | 用途 |
|------|------|------|
| 桌面端（源码可运行） | 本目录 | 导入/OCR/校对/组卷/复习/错题包 |
| 一键 Mock 启动 | `start_mock.bat` | **推荐先用**：无需下载 OCR 模型 |
| 真实 OCR 启动 | `start_windows.bat` | PaddleOCR（首次会下模型） |
| CLI 演示 | `start_cli_demo.bat` | 一条龙 Mock 导入演示 |
| Android 错题本 | 桌面文件夹里的 `粉笔采集伴侣-1.3.2.apk` | 手机端完整功能（debug 签名，仅本人侧载） |
| Windows 安装包 | `artifacts\windows\FenbiStudy-Windows-v1.0.0.zip` | 官方打包版（含 Installer） |
| 对照今知说明 | `docs\jinzhi-upgrade.md` | 能力差异与新命令 |

## 2. 5 分钟上手（Windows）

### 方式 A：最快体验（Mock OCR）

1. 安装 **Python 3.12 x64**（或 `uv python install 3.12`）
2. 双击 `start_mock.bat`（首次自动建 `.venv` 并装依赖）
3. 在界面批量导入截图，或运行 `start_cli_demo.bat` 看命令行闭环

### 方式 B：官方安装包

1. 解压 `artifacts\windows\FenbiStudy-Windows-v1.0.0.zip`
2. 运行 `FenbiStudy-Installer.exe`（会校验内嵌 `FenbiStudy.zip` 的 SHA-256）
3. 从开始菜单打开「粉笔学习数据处理系统」

### 方式 C：手机端完整错题本（推荐）

安装最新 APK 后，桌面图标是「丁真笔记本」。底部五个页就是全部功能：

1. **首页**：错题/待复习/已掌握统计，今日复习，试卷和薄弱知识点
2. **收题**：在已经登录的粉笔里采集已完成试卷（不登录粉笔）
3. **错题**：搜索、按试卷/分类/知识点筛选，点进去校对或标会了/还是错了
4. **复习**：预习 / 一刷 / 二刷 / 间隔 / 已掌握，可整轨刷题
5. **我的**：导入今知错题包或试卷 JSON，导出错题包 / 全部题目 / 错题卷 HTML

题干识别仍在电脑完成：采完可无线传到电脑，识别后再把试卷 JSON 或错题包导回手机复习。

### 方式 D：在已经登录的粉笔里采集

本软件**不会登录粉笔**，也不要账号密码。请先用粉笔官方 App 登录（和平时一样），再打开一套**已完成、带解析**的试卷。

1. 手机安装 `粉笔采集伴侣-1.3.2.apk` 或 `artifacts\android\fenbi-capture-1.3.2.apk`
2. 打开收题页，点「开始采集」或「点我进入开关页」。打开「丁真自动翻页」后按返回，会自动继续。系统不允许软件代开这个开关。
3. 点「开始采集（回到粉笔）」：授权录屏后会回到已经打开的粉笔
4. 留在已完成试卷里。登录页和其它 App 不会被点击，也不会自动填账号
5. 电脑端也可：`start_mock.bat` →「手机自动采集」→ 连接 USB 调试后开始。本卷结束后会自动停止，并用 OCR 分题进入校对。
6. **无线传入**：电脑点「手机无线传入」，复制配对码；手机采完后粘贴配对码，点「传到电脑并自动识别」。需同一 Wi-Fi，并允许 Python 通过防火墙。

### 方式 E：Android 采集（旧说明）

1. 手机开启 USB 调试，或使用模拟器  
2. `adb install -r artifacts\android\fenbi-capture-personal-release.apk`  
3. 打开「粉笔学习采集伴侣」→ 授权悬浮窗/录屏 → 新建任务采集  
4. 桌面端用 ADB / USB 取回 PNG 后导入

## 3. 核心工作流（设计目标）

```text
粉笔官方客户端（本人已完成试卷）
  → 截图 / ADB / Android 伴侣采集
  → OCR + 完整性检查（用户答案 / 官方答案 / 官方解析）
  → 人工校对（NEEDS_REVIEW → VERIFIED）
  → 分析统计 + 错题卷/答题卡/解析册 PDF
  → 今知兼容错题包 ZIP（可与今知生态互换）
  → 预习/一刷/二刷复习计划 + 知识点导图
```

硬规则：缺官方答案/解析 → 只能 `needs_review`，AI **不能**覆盖官方字段。

## 4. 今知对照后的升级点（本交付新增）

- **export-mistake-package / import-mistake-package**：`jinzhi-mistake-package` v1 兼容  
- **review-plan**：预习 / 一刷 / 二刷 / 间隔复习轨道  
- **knowledge-mindmap.md**：知识点复习导图  
- 启动脚本：`start_mock.bat`、改进的 `start_windows.bat`

详见 `docs\jinzhi-upgrade.md`。

## 5. 验证状态（本机已跑）

| 检查 | 结果 |
|------|------|
| 全量 pytest | **passed**（含错题包/复习单测） |
| CLI Mock 导入 | 通过，写出 `data\demo-study.db` |
| 组卷 PDF/HTML | 通过 `exports\demo_paper_bundle\` |
| 今知兼容 ZIP 导出/导入 | 通过 |
| 复习计划/导图 | 通过 |
| Android APK 模拟器安装冷启动 | 通过（见 `exports\emulator-smoke`） |

## 6. 隐私与边界

- 本地优先，默认不上传图库/凭据  
- 不做今知式云同步/订阅计费  
- 仅处理用户本人有权查看的学习内容  
- 不保存粉笔账号密码 / Cookie / Token  

## 7. 常用命令

```powershell
cd 本目录
.\.venv\Scripts\python.exe -m apps.desktop.main --cli --ocr-engine mock --import samples\golden\ordinary_single_choice\screen_01.png --group demo --kind question
.\.venv\Scripts\python.exe -m apps.desktop.main export-mistake-package --paper-json exports\paper_smoke\paper.json --package-zip exports\my-mistakes.zip
.\.venv\Scripts\python.exe -m apps.desktop.main review-plan --paper-json exports\paper_smoke\paper.json
.\.venv\Scripts\python.exe -m pytest -q
```
